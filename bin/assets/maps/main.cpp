#include <bits/stdc++.h>
#pragma GCC optimize ("O3")
#pragma GCC optimize ("unroll-loops")
using namespace std;
using ll = signed long long int;

constexpr size_t nmax = + 15;

int32_t main() {
    if (fopen("main.inp", "r")) {
        freopen("main.inp", "r", stdin);
        freopen("main.out", "w", stdout);
    }
    std::ios_base::sync_with_stdio(false);
    std::cin.tie(nullptr); std::cout.tie(nullptr);

    ifstream f ("floor1.txt");
    string s;
    f >> s; cout << s << "\n";
    f >> s; cout << s << "\n";
    f >> s; cout << s << "\n";
    f >> s; cout << s << "\n";
    f >> s; cout << s << "\n";
    f >> s; cout << s << "\n";
    f >> s; cout << s << "\n";
    f >> s; cout << s << "\n";

    return 0x0;
}
